<Window x:Class="Tetrahedron_Sierpinski.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tetrahedron_Sierpinski"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800"
        MouseMove="Window_MouseMove"
        MouseWheel="Window_MouseWheel"
        MouseDown="Window_MouseDown"
        MouseUp="Window_MouseUp">
    <Grid>
        <Viewport3D Name="viewport">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera"
                                   Position="6,6,10"
                                   LookDirection="-6,-6,-10"
                                   UpDirection="0,1,0"/>
            </Viewport3D.Camera>
        </Viewport3D>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Background="#88000000">
            <TextBox Name="maxLevelTextBox" Width="50" Text="6"/>
            <Button Content="Ustaw Max" Click="SetMax_Click"/>
            <Slider Name="levelSlider" Minimum="0" Maximum="6" Width="200"/>
            <Button Content="Start" Click="Start_Click"/>
            <Button Content="Stop" Click="Stop_Click"/>
        </StackPanel>
    </Grid>
</Window>
